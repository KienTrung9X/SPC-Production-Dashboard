// ═══════════════════════════════════════════════════════════════
// CONFIG - Tham số cấu hình cho Production Report
// ═══════════════════════════════════════════════════════════════

const config = {
    // ───────────────────────────────────────────────────────────
    // NGÀY THÁNG (Thay đổi ở đây)
    // ───────────────────────────────────────────────────────────
    startDate: 20250101,  // Ngày bắt đầu (YYYYMMDD)
    endDate: 20251231,    // Ngày kết thúc (YYYYMMDD)

    // ───────────────────────────────────────────────────────────
    // DÂY CHUYỀN (Thay đổi ở đây)
    // ───────────────────────────────────────────────────────────
    lineCode: '315',      // Mã dây chuyền (315, 312, 311, v.v.)

    // ───────────────────────────────────────────────────────────
    // SỐ BẢN GHI (Thay đổi ở đây)
    // ───────────────────────────────────────────────────────────
    rowLimit: 10000000,        // Số bản ghi tối đa (500, 1000, 10000, v.v.)

    // ───────────────────────────────────────────────────────────
    // DB2 CONNECTION (Thay đổi ở đây nếu cần)
    // ───────────────────────────────────────────────────────────
    provider: "IBMDA400.DataSource",
    hostname: "10.247.194.1",
    database: "WAVEDLIB",
    uid: "FA01001",
    pwd: "FA01001"
};

module.exports = config;
